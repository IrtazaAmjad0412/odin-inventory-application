<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <div class="item-list">
      <% items.forEach((item) => { %>
      <div class="item-card">
        <h2><%= item.name %></h2>
      </div>
      <% }) %>
    </div>
  </body>
</html>
